/// <summary>
/// Form overlay ekseninin yön tanımı.
/// Positive = overlay'ın birinci yönü, Negative = ikinci yönü, Either = her iki yön de kabul.
/// </summary>
public enum FormDirection
{
    Positive,
    Negative,
    Either
}
